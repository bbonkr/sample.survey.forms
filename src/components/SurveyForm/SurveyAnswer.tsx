import React from 'react';
import { Answer } from '../../models';

interface SurveyAnswerProps {
    record: Answer;
}

export const SurveyAnswer = ({ record }: SurveyAnswerProps) => {
    return <div></div>;
};
